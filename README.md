# CONCEPT-DRIFT-DETECTION-IN-DATA-STREAM-MINING
Proving Algorithm
to run follow the steps
1. run cmd
2. cd to project location
3. make sure python is installed
4. (python -m venv venv) run this command
5. run this command (venv\Scripts\activate)
6. install the following library pip install flask flask-cors river numpy pandas seaborn matplotlib scipy,pip install numpy
,pip install pandas
,pip install numpy pandas matplotlib seaborn river flask
after that run app.py in cmd ofcourse
you will get a link paste it on your browser
if you face an unexpected error send screen shots in the group
thanks
